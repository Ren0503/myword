export enum POST_TYPE {
  STORY = 'STORY',
  POST = 'POST',
  REEL = 'REEL',
}

export enum POST_MODE {
  PRIVATE = 'PRIVATE',
  FRIEND = 'FRIEND',
  PUBLIC = 'PUBLIC'
}

export enum POST_STATUS {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
  BANNED = 'BANNED'
}

export enum QUERY_POST_TYPE {
  COMMUNITY = 'COMMUNITY',
  GROUP = 'GROUP',
  USER = 'USER',
}